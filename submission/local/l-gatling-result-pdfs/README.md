# Item L - Gatling Result PDFs

- Status: ready for max-limit PDF; load/stress caveat remains
- Assignment item: `l) Attach 3 PDFs (max limit, load and stress) with Gatling results/graphs, explain why the results look this way`
- Packaged PDFs: `max-limit-report.pdf`, `load-5m-report.pdf`, `stress-5m-report.pdf`
- Supporting explanation: `graph-explanations.md`
- Source checked: `builds/max-limit-local/` for refreshed max-limit PDF and graph screenshot. Matching build `#17` load/stress PDFs were generated by Jenkins but were not retained in the build archive.

Validation: `max-limit-report.pdf` matches `builds/max-limit-local/max-limit-report.pdf` and differs from the public max-limit PDF.

The local graph screenshot shows `2340 active users`, `1399 OK`, and `0 KO`. Use that as the local max-limit graph point.

The load/stress CMD screenshots are refreshed under item `k` from local build `#17`, but the matching build `#17` Gatling graph PDFs are not available from retained Jenkins artifacts. Do not claim the local load/stress PDFs as build `#17` evidence unless those PDFs are re-exported from that run or the tests are rerun and packaged together. Terminal/CMD screenshots are packaged separately under item `k`.
