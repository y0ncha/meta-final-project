# Attachments Manifest

The attachments manifest has been consolidated into `docs/submission.md`.

Use `docs/submission.md` as the single source of truth for:

- the final email body,
- required assignment items `a` through `l`,
- required explanations,
- optional public-IP bonus evidence,
- final manual checks before sending.
